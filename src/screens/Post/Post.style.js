import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {},
  image: {
    width: '100%',
    aspectRatio: 3 / 2,
    resizeMode: 'cover',
    borderRadius: 10,
  },
});

export default styles;
